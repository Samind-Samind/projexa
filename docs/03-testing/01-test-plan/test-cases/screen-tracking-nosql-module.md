# Test Case — Screen Tracking Module (สโคปงานส่งหลักสูตร NoSQL)

- **วันที่สร้าง:** 2026-08-30
- **สถานะ:** Confirmed — ยืนยันโดย user เมื่อ 2026-08-30
- **อ้างอิง Acceptance Criteria:** [[../screen-tracking-nosql-module-acceptance-criteria|screen-tracking-nosql-module-acceptance-criteria]]
- **อ้างอิง Requirement:** [[../../../01-requirements/01-spec/20260824-009-scr-009-ทะเบียนหน้าจอ|SCR-009]],
  [[../../../01-requirements/01-spec/20260824-010-scr-010-รายละเอียดหน้าจอ|SCR-010]],
  [[../../../01-requirements/01-spec/20260824-013-scr-013-มอบหมายผู้รับผิดชอบ|SCR-013]],
  [[../../../01-requirements/01-spec/20260824-016-scr-016-บันทึกความก้าวหน้า|SCR-016]]

> **คำเตือนสำคัญ:** ไฟล์นี้ทดสอบ**สโคปงานส่งหลักสูตรที่ตัดลดแล้วเท่านั้น**
> (3 สถานะ, `assignees[]` แบบ embedded) — คนละไฟล์กับ
> [[scr-009-ทะเบียนหน้าจอ|scr-009-ทะเบียนหน้าจอ.md]],
> [[scr-010-รายละเอียดหน้าจอ|scr-010-รายละเอียดหน้าจอ.md]],
> [[scr-013-มอบหมายผู้รับผิดชอบ|scr-013-มอบหมายผู้รับผิดชอบ.md]],
> [[scr-016-บันทึกความก้าวหน้า|scr-016-บันทึกความก้าวหน้า.md]] ซึ่งทดสอบระบบ
> Projexa จริงเต็มรูปแบบ ห้ามนำ 2 ชุดนี้มาปนกันหรือใช้แทนกัน — เลข TC ในไฟล์
> นี้ต่อจากเลขรันนิ่งสูงสุดของโปรเจกต์ (TC-083) เริ่มที่ **TC-084**

## SCR-009 ทะเบียนหน้าจอ (list)

### TC-084 แสดงรายการหน้าจอจาก collection `screens`

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | มีเอกสารใน collection `screens` อย่างน้อย 3 รายการ แต่ละรายการมี `type`, `current_status`, `assignees[]` ครบ |
| Test Steps | 1. เข้าสู่ระบบด้วยบัญชี SA<br>2. เปิดหน้าทะเบียนหน้าจอ |
| Expected Result | ระบบแสดงรายการหน้าจอทั้ง 3 รายการ พร้อม `type.label`, `current_status`, และผู้รับผิดชอบจาก `assignees[]` ครบถ้วนโดยไม่มี query join เพิ่ม |
| Test Data | เอกสาร screens ตัวอย่าง 3 รายการ (ประเภท/สถานะต่างกัน) |
| อ้างอิง AC | AC-NOSQL-009-1 |
| อ้างอิง Requirement | SCR-009 |

### TC-085 กรองตามประเภทและสถานะ (3 ค่า)

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | collection `screens` มีเอกสารหลายประเภทและหลายสถานะในกลุ่ม Not Started/Analysis/Design |
| Test Steps | 1. เปิดหน้าทะเบียนหน้าจอ<br>2. เลือก filter ประเภท = "Process"<br>3. เลือก filter สถานะ = "Analysis" |
| Expected Result | ระบบแสดงเฉพาะเอกสารที่ `type.code = PROCESS` และ `current_status = Analysis` |
| Test Data | เอกสาร screens ที่มีประเภท/สถานะผสมกันอย่างน้อย 5 รายการ |
| อ้างอิง AC | AC-NOSQL-009-2 |
| อ้างอิง Requirement | SCR-009 |

### TC-086 Empty state เมื่อ collection ว่าง

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | collection `screens` ไม่มีเอกสารเลย |
| Test Steps | 1. เข้าสู่ระบบด้วยบัญชี SA<br>2. เปิดหน้าทะเบียนหน้าจอ |
| Expected Result | ระบบแสดง empty state พร้อมปุ่ม "สร้างหน้าจอใหม่" |
| Test Data | ไม่มี (collection ว่าง) |
| อ้างอิง AC | AC-NOSQL-009-3 |
| อ้างอิง Requirement | SCR-009 |

### TC-087 ไม่พบผลลัพธ์ตาม filter

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | collection `screens` มีเอกสารอยู่ แต่ไม่มีรายการใดตรงกับเงื่อนไข filter ที่จะทดสอบ |
| Test Steps | 1. เปิดหน้าทะเบียนหน้าจอ<br>2. เลือก filter ประเภท/สถานะที่ไม่มีเอกสารตรงเงื่อนไข |
| Expected Result | ระบบแสดงข้อความ "ไม่พบหน้าจอที่ตรงเงื่อนไข" พร้อมปุ่มล้าง filter |
| Test Data | เอกสาร screens ที่มีอยู่ไม่ตรงกับ filter ที่เลือก |
| อ้างอิง AC | AC-NOSQL-009-4 |
| อ้างอิง Requirement | SCR-009 |

## SCR-010 รายละเอียดหน้าจอ (สร้าง/แก้ไข + AI แนะนำประเภท)

### TC-088 บันทึกหน้าจอใหม่พร้อม denormalized type.label

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | collection `screenTypes` มีข้อมูลประเภทอย่างน้อย 1 รายการ (เช่น PROCESS/Process) |
| Test Steps | 1. เปิด "สร้างหน้าจอใหม่"<br>2. กรอกชื่อ + คำอธิบาย<br>3. เลือกประเภทจาก dropdown เอง (ไม่ใช้ AI)<br>4. กด "บันทึก" |
| Expected Result | เอกสารใหม่ถูก upsert ใน `screens` โดยฟิลด์ `type` มีทั้ง `type_id` และ `label` ติดไปด้วย (denormalized) |
| Test Data | ชื่อหน้าจอ "ทดสอบ TC-088", ประเภท PROCESS |
| อ้างอิง AC | AC-NOSQL-010-1 |
| อ้างอิง Requirement | SCR-010 |

### TC-089 AI แนะนำประเภทพร้อม confidence แล้ว SA ต้องกดยืนยันก่อนบันทึก

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | AI Type Suggestion Helper พร้อมให้บริการ |
| Test Steps | 1. เปิด "สร้างหน้าจอใหม่"<br>2. พิมพ์ชื่อ + คำอธิบายหน้าจอ<br>3. เลือกให้ AI ช่วยแนะนำประเภท<br>4. สังเกตหน้าจอก่อนกดยืนยัน |
| Expected Result | ระบบแสดงประเภทที่แนะนำพร้อม `ai_confidence` เป็นตัวเลือกที่ยังไม่บันทึกลง DB จนกว่า SA จะกดยืนยัน (หรือเลือกประเภทอื่นเอง) แล้วกด "บันทึก" อีกครั้ง — `is_suggested = true` ก่อนยืนยัน |
| Test Data | คำอธิบายหน้าจอที่ระบุ intent ชัดเจน เช่น "หน้าจอสำหรับค้นหาและแสดงรายการ" |
| อ้างอิง AC | AC-NOSQL-010-2 |
| อ้างอิง Requirement | SCR-010 |

### TC-090 AI timeout ไม่บล็อกการทำงาน

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | จำลอง AI Type Suggestion Helper ให้ timeout/ล้มเหลว |
| Test Steps | 1. เปิด "สร้างหน้าจอใหม่"<br>2. พิมพ์ชื่อ + คำอธิบาย<br>3. เลือกให้ AI ช่วยแนะนำประเภท (จะ timeout)<br>4. เลือกประเภทจาก dropdown เอง<br>5. กด "บันทึก" |
| Expected Result | ระบบไม่บล็อกการทำงาน แสดงว่า AI แนะนำไม่สำเร็จ และ SA บันทึกหน้าจอได้ปกติด้วยประเภทที่เลือกเอง |
| Test Data | เหมือน TC-089 แต่ AI ตอบสนอง timeout |
| อ้างอิง AC | AC-NOSQL-010-3 |
| อ้างอิง Requirement | SCR-010 |

### TC-091 บังคับกรอกชื่อหน้าจอก่อนบันทึก

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | เปิดฟอร์มสร้างหน้าจอใหม่ |
| Test Steps | 1. เว้นช่องชื่อหน้าจอว่างไว้<br>2. กรอกคำอธิบายและเลือกประเภท<br>3. กด "บันทึก" |
| Expected Result | ระบบบล็อกการบันทึกและแจ้งเตือนที่ field ชื่อว่าเป็น field บังคับ |
| Test Data | ชื่อหน้าจอ = ว่าง |
| อ้างอิง AC | AC-NOSQL-010-4 |
| อ้างอิง Requirement | SCR-010 |

### TC-092 ตรวจจับการบันทึกชนกัน (optimistic concurrency)

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | SA เปิดฟอร์มแก้ไขหน้าจอที่มีอยู่แล้ว 1 รายการ |
| Test Steps | 1. SA เปิดฟอร์มแก้ไขหน้าจอ SCR ตัวอย่าง<br>2. จำลองผู้ใช้อื่นแก้ไขและบันทึกเอกสารเดียวกันสำเร็จก่อน<br>3. SA กด "บันทึก" ด้วยข้อมูลที่โหลดไว้ตั้งแต่ต้น |
| Expected Result | ระบบตรวจพบว่าเอกสารถูกแก้ไปแล้ว แจ้งเตือนให้ SA โหลดข้อมูลล่าสุดก่อนบันทึกซ้ำ ไม่เขียนทับข้อมูลของผู้ใช้อื่นเงียบๆ |
| Test Data | เอกสาร screens 1 รายการที่ถูกแก้ไข 2 ครั้งพร้อมกัน |
| อ้างอิง AC | AC-NOSQL-010-5 |
| อ้างอิง Requirement | SCR-010 |

## SCR-013 มอบหมายผู้รับผิดชอบ (`assignees[]` แบบ embedded)

### TC-093 มอบหมายผู้รับผิดชอบให้หน้าจอเดียว

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | มีหน้าจอ 1 รายการที่ยังไม่มีผู้รับผิดชอบ role ที่จะมอบหมาย |
| Test Steps | 1. PM เปิดหน้าจอที่จะมอบหมาย<br>2. เลือกผู้รับผิดชอบ + บทบาท (เช่น Dev)<br>3. กด "มอบหมาย" |
| Expected Result | field `assignees[]` ของเอกสารนั้นมี entry ใหม่ `{user_id, user_name, role, assigned_by, assigned_at}` เพิ่มเข้ามา — `user_name` ต้องตรงกับชื่อผู้ใช้ที่เลือก (denormalized snapshot) |
| Test Data | หน้าจอ 1 รายการ, ผู้ใช้ 1 คน, role = Dev |
| อ้างอิง AC | AC-NOSQL-013-1 |
| อ้างอิง Requirement | SCR-013 |

### TC-094 มอบหมายหลายหน้าจอพร้อมกันเป็นชุด

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | มีหน้าจออย่างน้อย 3 รายการที่ยังไม่มีผู้รับผิดชอบ role ที่จะมอบหมาย |
| Test Steps | 1. PM เลือกหน้าจอ 3 รายการพร้อมกัน<br>2. เลือกผู้รับผิดชอบ + บทบาทเดียวกันสำหรับทั้งชุด<br>3. กด "มอบหมาย" |
| Expected Result | `assignees[]` ของทั้ง 3 เอกสารถูกอัปเดต และ toast แสดง "มอบหมายแล้ว 3 หน้าจอ" |
| Test Data | หน้าจอ 3 รายการ, ผู้ใช้ 1 คน, role = Tester |
| อ้างอิง AC | AC-NOSQL-013-2 |
| อ้างอิง Requirement | SCR-013 |

### TC-095 มอบหมาย role ซ้ำเป็นการแทนที่ ไม่สร้างซ้ำ

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | หน้าจอมี entry ใน `assignees[]` ที่ role = Dev อยู่แล้ว (ผู้ใช้ A) |
| Test Steps | 1. PM มอบหมายผู้ใช้ B ให้ role Dev ของหน้าจอเดียวกัน<br>2. กด "มอบหมาย" |
| Expected Result | entry เดิมของผู้ใช้ A ใน role Dev ถูกแทนที่ด้วยผู้ใช้ B — อาร์เรย์ไม่มี entry role Dev ซ้ำ 2 รายการ |
| Test Data | หน้าจอ 1 รายการที่มี assignees[] = [{user: A, role: Dev}] |
| อ้างอิง AC | AC-NOSQL-013-3 |
| อ้างอิง Requirement | SCR-013 |

### TC-096 ข้ามหน้าจอที่ถูกลบระหว่างมอบหมายเป็นชุด

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | PM เลือกหน้าจอ 3 รายการเพื่อมอบหมายเป็นชุด |
| Test Steps | 1. PM เริ่มมอบหมายหน้าจอ 3 รายการ<br>2. จำลองการลบเอกสารหน้าจอรายการที่ 2 ก่อนขั้นตอนบันทึกเสร็จ<br>3. กด "มอบหมาย" |
| Expected Result | หน้าจอรายการที่ 1 และ 3 มอบหมายสำเร็จ รายการที่ 2 ถูกข้าม และระบบแสดงแยกว่ารายการไหนสำเร็จ/ไม่สำเร็จ ไม่ยกเลิกรายการที่สำเร็จแล้ว |
| Test Data | หน้าจอ 3 รายการ โดย 1 รายการถูกลบระหว่างทำ |
| อ้างอิง AC | AC-NOSQL-013-4 |
| อ้างอิง Requirement | SCR-013 |

## SCR-016 บันทึกความก้าวหน้า (เปลี่ยนสถานะ → เขียน `statusHistory`)

### TC-097 เปลี่ยนสถานะเดินหน้าปกติ (ไม่บังคับเหตุผล)

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | หน้าจอมี `current_status = Not Started` |
| Test Steps | 1. ผู้รับผิดชอบเปิดหน้าบันทึกความก้าวหน้าของหน้าจอที่ตนรับผิดชอบ<br>2. เลือกสถานะ Analysis<br>3. กด "บันทึก" โดยไม่กรอกเหตุผล |
| Expected Result | `screens/{id}.current_status` เปลี่ยนเป็น Analysis สำเร็จ ไม่มีการบังคับกรอกเหตุผล |
| Test Data | หน้าจอ 1 รายการ สถานะเริ่มต้น Not Started |
| อ้างอิง AC | AC-NOSQL-016-1 |
| อ้างอิง Requirement | SCR-016 |

### TC-098 บังคับกรอกเหตุผลเมื่อถอยสถานะ

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | หน้าจอมี `current_status = Design` |
| Test Steps | 1. ผู้รับผิดชอบเปิดหน้าบันทึกความก้าวหน้า<br>2. เลือกสถานะ Analysis (ถอยหลัง)<br>3. กด "บันทึก" โดยไม่กรอกเหตุผล |
| Expected Result | ระบบบล็อกการบันทึกและบังคับแสดงช่อง "เหตุผล" ให้กรอกก่อน |
| Test Data | หน้าจอ 1 รายการ สถานะเริ่มต้น Design |
| อ้างอิง AC | AC-NOSQL-016-2 |
| อ้างอิง Requirement | SCR-016 |

### TC-099 เขียนเอกสารใหม่ใน subcollection statusHistory พร้อม changed_by/changed_at อัตโนมัติ

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | หน้าจอมี `current_status = Not Started` |
| Test Steps | 1. ผู้รับผิดชอบเปลี่ยนสถานะเป็น Analysis และกด "บันทึก"<br>2. เปิดดู timeline ประวัติของหน้าจอนั้น |
| Expected Result | subcollection `screens/{id}/statusHistory` มีเอกสารใหม่ที่มี `old_status = Not Started`, `new_status = Analysis`, `changed_by` และ `changed_at` ถูกบันทึกโดยระบบอัตโนมัติ (ไม่ใช่ผู้ใช้กรอกเอง) |
| Test Data | หน้าจอ 1 รายการ, ผู้ใช้ที่ล็อกอินคือผู้รับผิดชอบ |
| อ้างอิง AC | AC-NOSQL-016-3 |
| อ้างอิง Requirement | SCR-016 |

### TC-100 หน้าจอถูกลบไปแล้วก่อนกดบันทึก

| ฟิลด์ | รายละเอียด |
|---|---|
| Pre-condition | ผู้รับผิดชอบเปิดหน้าบันทึกความก้าวหน้าของหน้าจอ SCR ตัวอย่างค้างไว้ |
| Test Steps | 1. เปิดหน้าบันทึกความก้าวหน้า<br>2. จำลองการลบเอกสาร `screens/{id}` นั้นจากที่อื่น<br>3. ผู้รับผิดชอบเลือกสถานะใหม่แล้วกด "บันทึก" |
| Expected Result | ระบบแจ้งเตือนว่าหน้าจอนี้ไม่มีอยู่แล้ว และพากลับไปหน้ารายการ ไม่มีเอกสารใหม่ถูกสร้างใน statusHistory |
| Test Data | หน้าจอ 1 รายการที่ถูกลบระหว่างทำ |
| อ้างอิง AC | AC-NOSQL-016-4 |
| อ้างอิง Requirement | SCR-016 |

---

> เอกสารนี้เป็น**คนละแทร็ก**กับ test case ของระบบ Projexa จริง (`scr-009-...md`
> ถึง `scr-016-...md` ในโฟลเดอร์เดียวกัน) — ถ้าจะเขียนโค้ดส่งงานหลักสูตร NoSQL
> ให้ยึด TC-084 ถึง TC-100 ในไฟล์นี้เท่านั้น
