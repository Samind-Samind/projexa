# Log — 2026-08-26

## สร้าง/อัปเดตเอกสาร High-Level Architecture

- เอกสาร: [[../02-design/02-technical/architecture|architecture.md]]
- Section ที่ทำรอบนี้: 1. ภาพรวมและ System Boundary (Context Diagram) —
  Confirmed (2026-08-26), 2. Component/Layer Breakdown — Confirmed
  (2026-08-26), 3. Data Flow ตาม User Journey (Flow A–D) — Confirmed
  (2026-08-26), 4. ขอบเขต AI Orchestration — Confirmed (2026-08-26),
  5. Cross-cutting Concerns — Confirmed (2026-08-26), 6. เทียบกับ §3 เดิม —
  Confirmed (2026-08-26)
- Sync §3: ไม่ได้ sync (ไม่มีความขัดแย้งกับ §3 เดิมของ
  `Projexa-System-Design-R1.md` — เอกสารใหม่เป็นการขยายรายละเอียดเท่านั้น)
- สรุป: ใช้ skill `architecture` มอบหมาย sub-agent `architecture-writer`
  สร้างเอกสาร [[../02-design/02-technical/architecture|architecture.md]]
  ใหม่ทั้งฉบับครั้งแรกในโปรเจกต์ (conceptual, ไม่ผูกกับเทคโนโลยี/framework
  ใดๆ) หลังผ่านการสัมภาษณ์และรีวิวกับ user จนยืนยันครบทุก section ("ยืนยัน
  ตามนี้") มติสำคัญที่ user ตัดสินใจ: (1) Component breakdown ยึดโครงสร้าง
  4 layer + 4 service เดิมของ §3 ตรงๆ ไม่แบ่งใหม่ตาม M1–M7/bounded context
  (SCR-025/026 ของ M7 จัดไว้ใน Project Service โดยประเมินแทนเพราะ §3 เดิมไม่
  ได้ระบุ mapping ไว้ชัด), (2) Multi-project เป็นแบบ single-instance ผูกทุก
  entity กับ `Project` และ filter สิทธิ์ที่ Application Layer (logical
  isolation ไม่ใช่ physical/multi-tenant), (3) Audit trail บังคับเฉพาะระดับ
  Screen (`StatusHistory`) เขียนแบบ synchronous ในธุรกรรมเดียวกับการเปลี่ยน
  สถานะเสมอ เพื่อรักษาสาย traceability, (4) ทุกจุดที่มี AI Orchestration ต้อง
  มี manual fallback path คู่ขนานเสมอ ไม่บล็อกงานถ้า AI ล้มเหลว, (5) ยึด
  consistency-first เมื่อ NFR ด้าน consistency ของสาย traceability ขัดกับ
  performance, (6) SSO และช่องทางแจ้งเตือนภายนอก (เช่น LINE/Email) แสดงเป็น
  placeholder เส้นประใน context diagram สำหรับ Phase 2 เท่านั้น ยังไม่
  implement ในเฟสนี้ — เทียบกับ §3 เดิมของ `Projexa-System-Design-R1.md` แล้ว
  สอดคล้องกันทั้งหมด ไม่มีจุดขัดแย้ง จึงไม่ sync/ไม่แก้ไข §3 ในรอบนี้ พร้อม
  อัปเดต [[../02-design/02-technical/index|02-design/02-technical/index.md]]
  ให้ลิงก์ไปยังเอกสารใหม่

## สร้าง/อัปเดตเอกสาร Database Schema / API Spec

- เอกสาร: [[../02-design/02-technical/database-schema|database-schema.md]]
  และ [[../02-design/02-technical/api-spec|api-spec.md]] (สร้างใหม่ทั้ง
  สองไฟล์ครั้งแรกในโปรเจกต์ — เนื้อหาครบสมบูรณ์แล้วหลังรอบที่ 2 ไม่มี `TODO`
  เหลืออยู่)
- ตาราง/resource ที่ทำรอบนี้: ครบสโคปตามที่ผู้เรียกระบุ — 24 ตารางใน
  database-schema.md (`Project`, `TorDocument`, `TorClause`, `Requirement`,
  `Milestone`, `DeliverableDoc`, `Module`, `Screen`, `ScreenRequirement`,
  `ScreenCapability`, `BusinessRule`, `ScreenFlow`, `Assignment`,
  `ScreenStatus`, `StatusHistory`, `Issue`, `TestCase`, `TestResult`,
  `Attachment`, `DocTemplate`, `GeneratedDocument`, `User`,
  `ProjectRoleAssignment`, `MasterDataItem`) และ resource ครบ 7 โมดูล
  (M1–M7, 26 หน้าจอ) ใน api-spec.md — ทุกตาราง/resource สถานะ
  `Draft (suggested)` ตามมติของ user (ยังไม่มีรายการใดเป็น `Confirmed`)
- Sync §4.1: ทำแล้ว — เพิ่ม 5 แถวใหม่ใน `Projexa-System-Design-R1.md`
  (`User`, `ProjectRoleAssignment`, `MasterDataItem`, `ScreenRequirement`,
  `ScreenFlow`) พร้อมเพิ่มหมายเหตุลิงก์ไปยัง database-schema.md/api-spec.md
  ต่อท้ายตาราง §4.1
- สรุป (รอบที่ 1): ใช้ skill `data-api-design` มอบหมาย sub-agent
  `data-api-writer` สร้างเอกสาร database-schema.md และ api-spec.md ใหม่ทั้ง
  ฉบับครั้งแรก บันทึกบริบท/มติที่ user ยืนยันแล้วครบ 11 ข้อ (PK strategy, AI
  result storage แบบผสม, soft-delete policy ยกเว้น StatusHistory,
  GeneratedDocument versioning แบบ Draft→Exported immutable, endpoint style
  แบบผสม resource+action-oriented, AI job async+polling, คู่ ai-propose/
  confirm บังคับทุก AI endpoint, pagination/filter/sort มาตรฐานเดียว, error
  response มาตรฐานเดียว, ยังไม่กำหนด API versioning, permission model อิง
  ProjectRoleAssignment + เสริม Assignment ระดับ record เฉพาะจุด) — รอบนี้
  เนื้อหาฉบับเต็ม (ER Diagram, field list รายตาราง, ตาราง resource/
  operation) ยังไม่ถูกส่งมาให้ sub-agent จริง จึงทำเครื่องหมาย `TODO` ไว้
  ทุกจุดที่ขาดตามกติกาที่ห้าม sub-agent ออกแบบฟิลด์/ความสัมพันธ์/endpoint
  เพิ่มเติมเอง
- สรุป (รอบที่ 2 — ปิดงาน): ผู้เรียกส่งเนื้อหาฉบับเต็มที่ user ยืนยันแล้วมา
  ให้ครบ — เติม ER Diagram (mermaid, ครบ 24 ตาราง พร้อมหมายเหตุเรื่อง
  `Attachment` แบบ polymorphic), รายละเอียดฟิลด์ครบทุกตาราง (24 ตาราง) พร้อม
  mapping "ใช้งานโดยหน้าจอ" ต่อตาราง ใน database-schema.md; และตาราง
  resource/operation ครบทุก resource ของทั้ง 7 โมดูล (Method+Path เชิง
  แนวคิด, สิทธิ์ตาม Role, request/response เชิงแนวคิด) ใน api-spec.md แทนที่
  `TODO` ทั้งหมดเรียบร้อยแล้ว ทุกตาราง/resource ยังคงสถานะ
  `Draft (suggested)` ตามเดิม (ยังไม่มีรายการใดเป็น `Confirmed`) คู่
  `ai-propose`/`confirm` และ endpoint async (`GET /ai-jobs/{job_id}`) ครบทุก
  จุดที่เกี่ยวข้องกับ AI Component (TOR Parser, Design Analyzer, Test
  Generator, Document Writer) ตามกติกา §7.2 ข้อ 5 และแยก endpoint "AI ร่าง
  เนื้อความบรรยาย" กับ "export/render จริง" ของ `GeneratedDocument` ตาม §8
  เรียบร้อย — ไม่มี `TODO` เหลืออยู่ในทั้งสองไฟล์
