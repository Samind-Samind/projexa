# Log — 2026-08-31

## เพิ่ม field `user_name` (denormalized) เข้า `assignees[]` — พบระหว่างเทียบ SCOPE.md กับตัวอย่าง LeaveEasy

- บริบท: user ให้เทียบตาราง `SCOPE.md` กับตัวอย่าง `LeaveEasy` ที่ทำในคาบ
  ทีละแถว (โฟลเดอร์หลัก/ประกอบ/ย่อย, ช่องบอกว่าเป็นของใคร, สถานะ, ช่องข้อความ
  ยาวที่ AI อ่าน) พบว่าแถว `requesterId`/`requesterName` (ช่องจดชื่อซ้ำไว้ –
  denormalize) ยังไม่มี field ที่ตรงกันใน `assignees[]` ของโมดูลนี้ — เดิมมีแค่
  `user_id` ไม่มีชื่อ ทั้งที่ `screen-tracking-nosql-module-sequence.md` §1 และ
  `screen-tracking-nosql-module-acceptance-criteria.md` (AC-NOSQL-009-1)
  เขียนไว้แล้วว่าหน้าทะเบียนหน้าจอแสดง "ผู้รับผิดชอบจาก `assignees[]`" แบบ
  denormalize โดยไม่ query join เพิ่ม — ถ้าไม่มีชื่อเก็บอยู่จริง ข้อความนั้นจะ
  ทำไม่ได้ตามที่เขียนไว้ user ยืนยันให้เพิ่ม `user_name` เข้าไปเพื่อปิดช่องว่างนี้
- เอกสาร/ไฟล์ที่แก้ไข (ทุกที่ที่อ้าง field shape ของ `assignees[]`):
  - `SCOPE.md` (แก้ไข — แถว "ช่องบอกว่าเป็นของใคร" เพิ่ม
    `assignees[].user_name`)
  - `docs/02-design/02-technical/screen-tracking-nosql-module.md` (แก้ไข —
    เพิ่ม `user_name` ในตาราง field ของ `screens.assignees`, บันทึกเหตุผลใน
    ประวัติการตัดสินใจ)
  - `docs/02-design/02-technical/screen-tracking-nosql-module-sequence.md`
    (แก้ไข — flow ที่ 3 (มอบหมายผู้รับผิดชอบ) payload PATCH เพิ่ม `user_name`,
    อัปเดตประวัติการอัปเดต)
  - `docs/03-testing/01-test-plan/screen-tracking-nosql-module-acceptance-criteria.md`
    (แก้ไข — AC-NOSQL-013-1 เพิ่ม `user_name` ใน entry ที่ push เข้า
    `assignees[]`)
  - `docs/03-testing/01-test-plan/test-cases/screen-tracking-nosql-module.md`
    (แก้ไข — TC-093 Expected Result เพิ่ม `user_name` พร้อมเงื่อนไขตรวจสอบ)
- ไม่ได้แก้: prototype `v2/scr-013.html`/`v2/script.js` — ตรวจแล้วพบว่า UI เดิม
  แสดงชื่อผู้รับผิดชอบ (`assignee-name`) อยู่แล้วโดยไม่มีการ hardcode field
  shape แบบ object literal ในโค้ด (เป็นการจำลอง DOM state ล้วนๆ) จึงไม่มีจุดที่
  ขัดกับ field ใหม่ ไม่ต้องแก้ตามหลัก self-contained snapshot ของเวอร์ชัน
- สรุป: `assignees[]` ของโมดูล Screen Tracking (สโคปงานส่งหลักสูตร NoSQL) มี
  field ครบ `{user_id, user_name, role, assigned_by, assigned_at}` แล้ว —
  `user_name` เป็น denormalized snapshot ตรงกับ pattern `requesterName` ของ
  LeaveEasy และ pattern `type.label` ที่มีอยู่แล้วในเอกสารเดิม เอกสารทุกไฟล์ที่
  อ้าง field shape นี้ sync ตรงกันแล้วทั้งหมด

## เปลี่ยนมติเทคโนโลยี + สร้าง `app/` — ต่อ Screen Tracking module เข้า Firestore project จริง

- บริบท: user ให้เขียนแผนเชื่อม "เว็บ my-projexa" เข้ากับ Firestore โดยส่ง
  `firebaseConfig` ของ project จริง (`projexa-b3a6a`) มาด้วย — ตรวจสอบแล้วพบว่า
  ขัดกับมติเดิมใน `screen-tracking-nosql-module-tech.md` (Node.js +
  `firebase-admin` + Firebase Local Emulator Suite, ไม่ต่อ project จริง) ถาม
  user แล้วยืนยันให้ **เปลี่ยนมติ** เป็น Client-side Firebase JS SDK ต่อ project
  จริงแทน (แบบเดียวกับตัวอย่าง `LeaveEasy` ในคาบ)
- เอกสารที่แก้ไข: `screen-tracking-nosql-module-tech.md` (แก้ตาราง "การ
  ตัดสินใจ" + "Dev / Test Environment" เป็น Client-side Web SDK + project จริง,
  เพิ่มประวัติการตัดสินใจอธิบายเหตุผลการเปลี่ยนมติ)
- โค้ดที่สร้างใหม่: โฟลเดอร์ `app/` ที่ root ของ repo (แยกจาก `docs/` และไม่แตะ
  prototype `v2/` ตามกฎ self-contained snapshot) — เป็นโค้ดจริงชุดแรกของ
  โปรเจกต์นี้ ครอบคลุม SCR-009/010/013/016 ต่อ Firestore project `projexa-b3a6a`
  จริง (อ่าน/เขียน `screens`, `screenTypes`, `users`, subcollection
  `statusHistory`) พร้อม `seed.html` ใส่ข้อมูลตัวอย่างและ `firestore.rules`
  แบบเปิด read/write (ยังไม่มี auth ในสโคปนี้ตาม `SCOPE.md`)
- ไม่ได้แก้: `SCOPE.md`, `screen-tracking-nosql-module.md` (โครงสร้างข้อมูล/
  field ไม่เปลี่ยน เปลี่ยนเฉพาะว่าจะ "รันที่ไหน/ด้วยภาษาอะไร" เท่านั้น)
