# Log — 2026-08-30

## เลือกโมดูลถัดไปสำหรับงานส่งหลักสูตร NoSQL + สร้างเอกสารขอบเขต

- เอกสาร:
  - `SCOPE.md` (สร้างใหม่ — repo root)
  - `docs/02-design/02-technical/screen-tracking-nosql-module.md` (สร้างใหม่)
  - `docs/02-design/02-technical/index.md` (แก้ไข — เพิ่มลิงก์ไปยัง
    screen-tracking-nosql-module)
- บริบท: user มีงานส่งหลักสูตร NoSQL ที่ต้องเลือกโมดูลจากโปรเจกต์จริง (Projexa)
  มาพัฒนาให้ตรงเงื่อนไขขอบเขตที่กำหนด (1 โฟลเดอร์หลัก, 2 โฟลเดอร์ประกอบ, 1
  โฟลเดอร์ย่อย, 3–4 หน้าจอ, 2–3 บทบาท, 3 สถานะ) โดยเทียบกับตัวอย่าง `LeaveEasy`
  ที่ทำในคาบ
- ทางเลือกที่พิจารณา: Requirement (ทะเบียนความต้องการ), Issue (ติดตามปัญหา),
  GeneratedDocument (สร้างเอกสารส่งมอบ), Screen (ทะเบียนหน้าจอ) — เลือก
  **Screen** เพราะ entity นี้มีโครงสร้างตรงกับเงื่อนไขมากที่สุดอยู่แล้วใน
  database-schema.md (`type` เป็น reference ไปตาราง list, `ScreenStatus` เป็น
  status เปลี่ยนได้หลายค่า, `StatusHistory` บังคับกรอก reason ตอนถอยสถานะ)
- มติสำคัญที่ user ยืนยัน:
  1. หน้าจอในสโคป: SCR-009 (ทะเบียนหน้าจอ), SCR-010 (รายละเอียดหน้าจอ), SCR-013
     (มอบหมายผู้รับผิดชอบ), SCR-016 (บันทึกความก้าวหน้า) — ตัด SCR-004/005/006/
     008 ออกเพราะเป็นสาย Requirement/TOR คนละโฟลเดอร์หลัก
  2. `Assignment` (ที่ SCR-013 ใช้) ให้ทำเป็น **embedded array** `assignees[]`
     ใน document `screen` แทนตารางแยก เพื่อไม่ให้เกิน "โฟลเดอร์ย่อย 1 อัน"
     (โฟลเดอร์ย่อยจริงมีแค่ `statusHistory`)
  3. **`database-schema.md`/`api-spec.md` ของจริงไม่ถูกแก้ไขเลย** — ยังคงมี
     `ScreenStatus` ครบทุกค่าเดิมและ `Assignment` เป็นตารางแยกตามเดิม เพราะเป็น
     ดีไซน์ระบบจริงของ Projexa ไม่ใช่สโคปที่ถูกตัดสำหรับงานส่ง — สโคปที่ตัดไว้
     ทั้งหมดถูกบันทึกแยกไว้ที่เอกสารใหม่ (`screen-tracking-nosql-module.md`)
     เท่านั้น
  4. สถานะที่ใช้จริงในรอบนี้: 3 ค่า (Not Started/Analysis/Design) เป็น subset
     ของ enum เต็มใน database-schema.md — ยืนยันแล้วว่าการเพิ่มสถานะกลับมาใน
     อนาคตไม่กระทบโครงสร้าง เพราะเป็น enum ระดับแอปพลิเคชัน ไม่ใช่ constraint
     ระดับ schema
  5. งานที่ AI ช่วย (สัปดาห์ที่ 8): แนะนำ `screenType` จากคำอธิบายหน้าจอ พร้อม
     confidence ให้ SA ยืนยัน — เลือกเพราะ reuse field `ai_confidence`/
     `origin_label`/`is_suggested` ที่มีอยู่แล้วใน `Screen` โดยไม่ต้องเพิ่ม
     field ใหม่ และตรงกับกติกา human-in-the-loop ของระบบ (§7.2)
- สรุป: สร้าง `SCOPE.md` (ตารางสรุปขอบเขตแบบสั้นตามฟอร์แมตหลักสูตร) และ
  `docs/02-design/02-technical/screen-tracking-nosql-module.md` (รายละเอียด
  NoSQL concrete design เต็มรูปแบบ พร้อม mermaid ER diagram, field ของแต่ละ
  collection, หน้าจอ/บทบาท/สถานะในสโคป, และรายการขอบเขตที่ยังไม่ทำ) — ยังไม่ได้
  เริ่มทำ Detailed Design (sequence diagram) ของ 4 หน้าจอ รอ user สั่งขั้นตอน
  ถัดไป

## สร้าง/อัปเดตเอกสาร Detailed Design

- SCR ในสโคป: SCR-009 (ทะเบียนหน้าจอ), SCR-010 (รายละเอียดหน้าจอ), SCR-013
  (มอบหมายผู้รับผิดชอบ), SCR-016 (บันทึกความก้าวหน้า)
- ไฟล์:
  - `docs/02-design/02-technical/detailed-design/scr-009-ทะเบียนหน้าจอ.md`
    (สร้างใหม่)
  - `docs/02-design/02-technical/detailed-design/scr-010-รายละเอียดหน้าจอ.md`
    (สร้างใหม่)
  - `docs/02-design/02-technical/detailed-design/scr-013-มอบหมายผู้รับผิดชอบ.md`
    (สร้างใหม่)
  - `docs/02-design/02-technical/detailed-design/scr-016-บันทึกความก้าวหน้า.md`
    (สร้างใหม่)
  - `docs/02-design/02-technical/detailed-design/index.md` (แก้ไข — เพิ่ม
    แถวลิงก์ทั้ง 4 ไฟล์ต่อจาก SCR-003 เดิม)
- SCR ที่มี AI Component เกี่ยวข้อง: SCR-010 (Design Analyzer, Gate 2 — ไม่ได้
  เรียกงาน AI ใหม่ในหน้านี้ เป็นจุดแสดงผลที่ AI เสนอไว้แล้วจาก SCR-008 ให้ SA
  ตรวจทานและยืนยันก่อนเปลี่ยนสถานะเป็น Confirmed) ส่วน SCR-009/013/016 ไม่มี
  AI component เกี่ยวข้อง
- สรุป: จัดทำ Detailed Design (Conceptual Design + Sequence Diagram) ครบ 4
  หน้าจอของโมดูล Screen Tracking ตามระดับความละเอียดที่ตกลงกัน คือ sequence
  diagram เดียวต่อ SCR (happy path ใช้ `alt`/`opt`/`loop`/`note` แยก branch
  ย่อยตามความเหมาะสม) พร้อมสรุป error/exception flow แบบ bullet list โดยไม่
  ทำ diagram แยกสำหรับ error path และไม่ทำ component-interaction diagram
  แยกต่างหาก — เนื้อหา conceptual design + sequence flow ทั้งหมดออกแบบและ
  ยืนยันโดย user ไว้แล้วในเซสชันก่อนหน้า (อ้างอิงจาก prototype ภายนอก Claude
  Design canvas ที่ user สร้างไว้ ไม่ใช่จาก `docs/02-design/01-prototypes/`
  ของโปรเจกต์) พบประเด็นเปิด 1 จุดที่ SCR-013 (ฟิลด์ `due_date` ยังไม่มีที่
  จัดเก็บชัดเจนใน `Assignment` schema ปัจจุบัน — บันทึกเป็น TODO ในเอกสารแล้ว
  รอ sync ผ่าน skill `data-api-design` ในรอบถัดไป)

## Sync ฟิลด์ due_date เข้า Assignment (Database Schema / API Spec)

- เอกสาร:
  - [[../02-design/02-technical/database-schema|database-schema.md]] (แก้ไข)
  - [[../02-design/02-technical/api-spec|api-spec.md]] (แก้ไข)
- ตาราง/resource ที่ทำรอบนี้:
  - `Assignment` (database-schema.md) — สถานะ `Draft (suggested)` (ไม่เปลี่ยน)
    — เพิ่มฟิลด์ `due_date` (DateTime, nullable) ต่อจากแถว `role`
  - `Assignment` (api-spec.md) — สถานะ `Draft (suggested)` (ไม่เปลี่ยน) — แก้
    request body ของ operation "Batch assign" (`POST /assignments/batch`)
    จาก `[{screen_id, user_id, role}]` เป็น `[{screen_id, user_id, role,
    due_date?}]`
- Sync §4.1/§7.1: ไม่ได้ sync (user ยืนยันว่าเป็นแค่เพิ่มฟิลด์ในตารางเดิม
  ไม่ใช่ entity/AI component ใหม่ ไม่ต้อง sync เอกสารหลักรอบนี้)
- บริบท: sync ตามประเด็นเปิดที่พบตอนทำ Detailed Design ของ
  [[../02-design/02-technical/detailed-design/scr-013-มอบหมายผู้รับผิดชอบ|SCR-013
  detailed design]] — ฟอร์ม batch assign ใน prototype มีการตั้งวันส่งงาน
  พร้อมกับมอบหมายผู้รับผิดชอบ ซึ่งไม่มีที่จัดเก็บในตาราง `Assignment` เดิม
  user ยืนยันให้เพิ่มฟิลด์ `due_date` ตรงเข้า `Assignment` โดยตรง (ผูกกับ
  screen×role×user แต่ละอัน แยกจาก `ScreenStatus.planned_end_date` ซึ่งเป็น
  กำหนดส่งของหน้าจอโดยรวม) ไม่มีการแก้ไข `docs/02-design/02-technical/index.md`
  เพราะไม่มีตาราง/resource ใหม่ ลิงก์เดิมยังถูกต้อง
- สรุป: เพิ่มฟิลด์ `due_date` (DateTime, nullable) เข้าตาราง `Assignment` และ
  แก้ request body ของ `POST /assignments/batch` ให้รองรับ `due_date?` แล้ว
  ทั้งสองไฟล์ยังคงสถานะ `Draft (suggested)` เหมือนเดิม
- Follow-up ที่ยังไม่ทำ (นอกสโคปรอบนี้): ควรกลับไปอัปเดตหัวข้อ "## 6.
  ประเด็นเปิด / TODO" ในไฟล์
  `docs/02-design/02-technical/detailed-design/scr-013-มอบหมายผู้รับผิดชอบ.md`
  ให้ระบุว่าประเด็นนี้ sync เข้า database-schema.md/api-spec.md แล้ว
- Follow-up นี้ทำแล้วในรอบถัดมา (แก้เองใน main loop ไม่ผ่าน sub agent เพราะ
  เป็นแค่อัปเดตสถานะ TODO ไม่ใช่การออกแบบใหม่): ปิดประเด็นเปิดใน
  `scr-013-มอบหมายผู้รับผิดชอบ.md` §6 พร้อมอ้างอิงกลับมาที่ commit นี้

## ตรวจสอบความสอดคล้องกับ SCOPE.md + สร้าง Sequence Design ของสโคปที่ตัดลดแล้ว

- บริบท: user ขอให้ตรวจว่า Detailed Design ของ SCR-009/010/013/016
  (`docs/02-design/02-technical/detailed-design/`) ตรงกับตารางขอบเขตใน
  [[../../SCOPE|SCOPE.md]] หรือไม่ — ตรวจแล้วพบว่า**ไม่ตรงกันหลายจุดโดยเจตนา**
  เพราะ Detailed Design อ้างอิงระบบจริงเต็มรูปแบบ (10 สถานะ, `Assignment`
  แยกตาราง, ประเภทหน้าจอ Dashboard/Form/Review/List/Detail/Timeline/Wizard)
  ในขณะที่ `SCOPE.md`/`screen-tracking-nosql-module.md` เป็นสโคปที่ตัดลดแล้ว
  สำหรับงานส่งหลักสูตร (3 สถานะ, `assignees[]` แบบ embedded, ประเภทหน้าจอ 5
  ค่า) — user ยืนยันให้แยกเป็น 2 เอกสารคนละเรื่องกัน (ไม่รวมหรือแก้ให้ตรงกัน)
- เอกสาร:
  - [[../../SCOPE|SCOPE.md]] (แก้ไข — เพิ่มหมายเหตุอธิบายว่า Detailed Design
    ของระบบจริงกับ SCOPE.md เป็นคนละแทร็กกันโดยเจตนา ต้องยึด SCOPE.md เท่านั้น
    ถ้าจะเขียนโค้ดส่งงานจริง)
  - `docs/02-design/02-technical/screen-tracking-nosql-module-sequence.md`
    (สร้างใหม่ — sequence diagram ครบ 4 flow ของสโคปที่ตัดลดแล้ว: รายการ
    หน้าจอ, รายละเอียด/สร้างหน้าจอ+AI แนะนำประเภท, มอบหมายผู้รับผิดชอบแบบ
    embedded, บันทึกความก้าวหน้า→เขียน statusHistory)
  - [[screen-tracking-nosql-module|screen-tracking-nosql-module.md]] (แก้ไข
    — เพิ่มลิงก์ไปยัง sequence design ไฟล์ใหม่)
  - `docs/02-design/02-technical/index.md` (แก้ไข — เพิ่มลิงก์ไฟล์ sequence
    design ใหม่)
- สรุป: ตอนนี้โมดูล Screen Tracking มีเอกสารครบ 2 แทร็กแยกกันชัดเจน — (1)
  ระบบจริง: `database-schema.md`/`api-spec.md`/`detailed-design/scr-009..016`
  (2) สโคปงานส่งหลักสูตร: `SCOPE.md`/`screen-tracking-nosql-module.md`/
  `screen-tracking-nosql-module-sequence.md` — เขียนโค้ดส่งงานจริงให้ยึดแทร็ก
  (2) เท่านั้น

## เพิ่มเอกสาร Tech Decision + Acceptance Criteria/Test Case ฉบับตัดสโคปให้ครบก่อนเขียนโค้ด

- บริบท: user ถามว่ายังขาดเอกสารอะไรก่อนจะพัฒนาโค้ดจริงตาม `SCOPE.md` — ตรวจ
  พบ 2 ช่องว่าง: (1) ยังไม่มีเอกสารตัดสินใจเทคโนโลยีของโมดูลนี้ (sequence doc
  ใช้ชื่อ layer แบบ generic เพราะ "ยังไม่มี tech-stack.md") (2)
  Acceptance Criteria/Test Case ของ SCR-009/010/013/016 ที่มีอยู่เดิมอ้างอิง
  ระบบ Projexa จริงเต็มรูปแบบ ไม่ตรงกับโครงสร้างตัดสโคป (3 สถานะ,
  `assignees[]` embedded, AI แนะนำ `screenType`+ยืนยัน, optimistic
  concurrency) — user ยืนยันให้ทำเอกสารเทคโนโลยีเป็นบันทึกสั้นเฉพาะโมดูล
  (ไม่ใช่ tech-stack.md เต็มระบบ) เลือก Firebase Firestore + Node.js
  (ตรงกับศัพท์ collection/subcollection/embedded array ที่เอกสารเดิมใช้อยู่
  แล้ว) และให้ AC/Test Case ฉบับตัดสโคปเป็น**ไฟล์แยกใหม่ต่างหาก** ไม่ปนกับ
  ไฟล์ระบบเต็มเดิม
- เอกสาร:
  - `docs/02-design/02-technical/screen-tracking-nosql-module-tech.md`
    (สร้างใหม่ — บันทึกตัดสินใจ Firebase Firestore + Node.js/`firebase-admin`
    SDK เฉพาะโมดูลนี้)
  - `docs/02-design/02-technical/screen-tracking-nosql-module-sequence.md`
    (แก้ไข — อัปเดตชื่อ layer จาก generic ให้ชี้ไปที่เอกสารเทคโนโลยีใหม่)
  - `docs/02-design/02-technical/index.md` (แก้ไข — เพิ่มลิงก์ไฟล์เทคโนโลยีใหม่)
  - `docs/03-testing/01-test-plan/screen-tracking-nosql-module-acceptance-criteria.md`
    (สร้างใหม่ — AC-NOSQL-009-x ถึง AC-NOSQL-016-x เขียนใหม่ตามโครงสร้างตัด
    สโคป ไม่ใช่ copy ของ acceptance-criteria.md เดิม)
  - `docs/03-testing/01-test-plan/test-cases/screen-tracking-nosql-module.md`
    (สร้างใหม่ — TC-084 ถึง TC-100 ต่อเลขรันนิ่งจาก TC-083 เดิมของโปรเจกต์)
  - `docs/03-testing/01-test-plan/index.md` (แก้ไข — เพิ่มลิงก์ไฟล์ AC ใหม่)
  - `docs/03-testing/01-test-plan/test-cases/index.md` (แก้ไข — เพิ่มลิงก์
    ไฟล์ test case ใหม่ในตารางแยกส่วน "สโคปงานส่งหลักสูตร NoSQL")
- สรุป: โมดูล Screen Tracking track (2) (งานส่งหลักสูตร NoSQL) มีเอกสารครบ
  ทุกระดับที่จำเป็นก่อนเขียนโค้ดจริงแล้ว — data design, sequence design, tech
  decision, acceptance criteria, test case ทั้งหมด**เป็น Draft** รอ SA/PM/
  Tester ยืนยันตามหลัก human-in-the-loop ก่อนถือเป็นฉบับใช้งานจริง
- **ยืนยันแล้ว (2026-08-30):** user ยืนยันเอกสารทั้ง 3 ไฟล์ตามที่สรุปไว้ข้างต้น
  — อัปเดตสถานะใน header จาก "Draft" เป็น "Confirmed — ยืนยันโดย user เมื่อ
  2026-08-30" ในไฟล์ `screen-tracking-nosql-module-tech.md`,
  `screen-tracking-nosql-module-acceptance-criteria.md`, และ
  `test-cases/screen-tracking-nosql-module.md` — พร้อมใช้เป็นฉบับอ้างอิงเขียน
  โค้ดจริงตาม `SCOPE.md` ได้แล้ว

## รอบตรวจซ้ำ — พบและแก้ 2 จุดก่อนเริ่มเขียนโค้ดจริง

- บริบท: user ขอให้ตรวจสอบอีกครั้งว่ายังมีเอกสารไหนต้องแก้ไข/ทำเพิ่มก่อนเริ่ม
  พัฒนาโค้ดจริง — ตรวจพบ 2 จุด:
  1. field `module_id` (Reference→Module) ยังค้างอยู่ในตาราง `screens` ของ
     `screen-tracking-nosql-module.md` ทั้งที่ `Module` ไม่ได้ถูกนับเป็น
     โฟลเดอร์ประกอบตาม §1/§2/ER diagram ของเอกสารเดียวกัน และไม่เคยถูกใช้ใน
     AC/Test Case ที่เขียนไว้แล้ว — เป็นฟิลด์ตกค้างจาก `database-schema.md`
     เดิมที่ `Screen belongs to Module` จริง
  2. `screen-tracking-nosql-module-tech.md` ตัดสินใจ engine/ภาษาไว้แล้วแต่ยัง
     ไม่ระบุ dev/test environment ที่จะใช้เขียน/รัน test จริง
- มติ user: (1) ตัด `module_id` ออกจากสโคปนี้ทั้งหมด (2) เพิ่ม Firebase Local
  Emulator Suite เป็น environment หลัก
- เอกสาร:
  - `docs/02-design/02-technical/screen-tracking-nosql-module.md` (แก้ไข —
    ลบแถว `module_id` ออกจากตาราง `screens`, เพิ่มหมายเหตุใน §7 ขอบเขตที่ยังไม่
    ทำ, อัปเดตประวัติการตัดสินใจ)
  - `docs/02-design/02-technical/screen-tracking-nosql-module-tech.md` (แก้ไข
    — เพิ่มหัวข้อ "Dev / Test Environment" ระบุ Firebase Local Emulator Suite,
    อัปเดตประวัติการตัดสินใจ)
- สรุป: หลังแก้ 2 จุดนี้ เอกสารทั้งชุดของ track งานส่งหลักสูตร NoSQL
  (`SCOPE.md`, `screen-tracking-nosql-module.md`,
  `screen-tracking-nosql-module-sequence.md`,
  `screen-tracking-nosql-module-tech.md`,
  `screen-tracking-nosql-module-acceptance-criteria.md`,
  `test-cases/screen-tracking-nosql-module.md`) ไม่พบจุดหลุดสโคป/ไม่สอดคล้อง
  กันอีก พร้อมเริ่มเขียนโค้ดจริงได้

## สร้าง/อัปเดต prototype: v2

- เวอร์ชัน: [[../02-design/01-prototypes/v2/index|v2]]
- สโคป: SCR-009 (ทะเบียนหน้าจอ), SCR-010 (รายละเอียดหน้าจอ), SCR-013
  (มอบหมายผู้รับผิดชอบ), SCR-016 (บันทึกความก้าวหน้า) — โมดูล Screen Tracking
  ตามขอบเขตที่ตัดลดไว้สำหรับงานส่งหลักสูตร NoSQL (`SCOPE.md`,
  `screen-tracking-nosql-module.md`) ไม่ใช่สโคประบบ Projexa จริงเต็มรูปแบบ
- DESIGN.md: ใช้ของเดิม (ไม่แก้ไขซ้ำในรอบนี้ — ถูกอัปเดตทั้งฉบับเป็น "Warm
  Terracotta Workspace" ให้ตรงกับไฟล์อ้างอิง `Projexa.html` ไปแล้วในรอบก่อน
  หน้านี้)
- ไฟล์:
  - `docs/02-design/01-prototypes/v2/style.css` (สร้างใหม่ — self-contained
    snapshot ของ token จาก DESIGN.md ปัจจุบัน: Sand & Cream + Terracotta &
    Sage, ฟอนต์ Mitr/Noto Sans Thai/Figtree, radius pill 999px สำหรับปุ่ม/
    แท็ก/status pill, การ์ดแบบ flat ไม่มี shadow, sidebar พื้นเขียวเข้ม
    `accent2-900`)
  - `docs/02-design/01-prototypes/v2/script.js` (สร้างใหม่ — รวม interaction
    utilities มาตรฐาน + ตรรกะเฉพาะโมดูล: filter/empty-state ของ SCR-009,
    AI suggest จำลอง + optimistic concurrency ของ SCR-010, batch assign
    แบบ embedded assignees[] ของ SCR-013, สลับสถานะ+เหตุผลแบบมีเงื่อนไข+
    timeline ของ SCR-016)
  - `docs/02-design/01-prototypes/v2/scr-009.html`,
    `scr-010.html`, `scr-013.html`, `scr-016.html` (สร้างใหม่ทั้งหมด)
  - `docs/02-design/01-prototypes/v2/index.html`,
    `docs/02-design/01-prototypes/v2/_meta.md` (สร้างใหม่)
  - `docs/02-design/01-prototypes/index.md` (แก้ไข — ชี้เวอร์ชันล่าสุดไปที่
    v2 พร้อมเก็บลิงก์ v1 เดิมไว้)
- สรุป: สร้าง prototype แบบ interactive ครบ 4 หน้าจอของโมดูล Screen Tracking
  (ขอบเขต NoSQL) ครอบคลุม AC-NOSQL-009-1 ถึง 016-3 เกือบทั้งหมด (ยกเว้น
  AC-NOSQL-016-4 หน้าจอถูกลบก่อนกดบันทึก ซึ่งไม่ได้อยู่ในรายละเอียดหน้าตาที่
  ระบุมาให้สร้างรอบนี้ — บันทึกเป็น TODO ใน `_meta.md`) เชื่อมลิงก์ครบทั้ง 4
  หน้าจอด้วย query string (`?screen=`, `?ids=`) แทนการ persist ข้อมูลข้ามการ
  reload หน้า ไม่แตะ `v1` (SCR-003) ตามหลัก self-contained snapshot

## ตรวจสอบ prototype v2 ในเบราว์เซอร์จริง + ปิดช่องโหว่ AC-NOSQL-016-4 + ยืนยัน

- บริบท: sub agent `prototype-builder` ไม่มีเครื่องมือเบราว์เซอร์ในตัว
  (verify ได้แค่ระดับโค้ด) จึงเปิด `v2/index.html` ผ่าน static preview server
  จริง (ตั้งค่าไว้ที่ `.claude/launch.json` จากตอนเปิดดู `Projexa.html`) แล้ว
  กดทดสอบทีละหน้าจอเอง: SCR-009 (filter ประเภท/สถานะ), SCR-010 (AI suggest +
  confirm, บังคับกรอกชื่อ, optimistic concurrency), SCR-013 (batch assign
  แทนที่ role เดิมไม่ซ้ำ), SCR-016 (3 สถานะ, บังคับเหตุผลเฉพาะถอยสถานะ,
  auto changed_by/changed_at) — ทำงานถูกต้องตรงตาม AC-NOSQL ทุกจุดที่ทดสอบ
- พบช่องโหว่ 1 จุดจากที่ `_meta.md` บันทึกไว้เป็น TODO: AC-NOSQL-016-4/TC-100
  (หน้าจอถูกลบไปแล้วก่อนกดบันทึก) ยังไม่ได้ implement ใน `scr-016.html`
- เอกสาร/ไฟล์ที่แก้ไขเพื่อปิดช่องโหว่:
  - `docs/02-design/01-prototypes/v2/scr-016.html` (แก้ไข — เพิ่มปุ่มทดสอบ
    "จำลองหน้าจอถูกลบไปแล้ว")
  - `docs/02-design/01-prototypes/v2/script.js` (แก้ไข — เพิ่ม state
    `screenDeletedSimulated` และเช็คก่อนบันทึกใน handler ของปุ่ม
    "บันทึกความก้าวหน้า": ถ้าจำลองว่าถูกลบแล้ว บล็อกการบันทึก แจ้ง toast
    danger แล้ว redirect กลับไป `scr-009.html`)
  - `docs/02-design/01-prototypes/v2/_meta.md` (แก้ไข — ปิด TODO นี้)
  - ทดสอบซ้ำในเบราว์เซอร์แล้วยืนยันว่ากดปุ่มจำลองแล้วกด "บันทึกความก้าวหน้า"
    บล็อกการบันทึกและพากลับไปหน้าทะเบียนหน้าจอได้ถูกต้องจริง
- **ยืนยันแล้ว (2026-08-30):** user ยืนยัน prototype v2 ตามที่สรุปผลไว้ —
  อัปเดตสถานะใน `docs/02-design/01-prototypes/v2/_meta.md` จาก "Draft" เป็น
  "Confirmed — ยืนยันโดย user เมื่อ 2026-08-30" (ไม่แตะสถานะของ `DESIGN.md`
  เพราะเป็นเอกสารระบบรวมที่ผ่านหลายรอบมาแล้วโดยยังไม่เคย Confirmed แยกต่างหาก
  ตามธรรมเนียมเดิมของไฟล์นี้)

## รอบตรวจซ้ำอีกครั้ง — พบและแก้ 2 จุดเล็กหลังยืนยัน prototype v2

- บริบท: user ขอให้ตรวจสอบอีกครั้งว่ายังมีเอกสารไหนต้องแก้ไข/ทำเพิ่มก่อนเริ่ม
  พัฒนาโค้ดจริง (รอบที่ 2 หลังยืนยัน v2) — ตรวจพบ 2 จุดเล็กที่ตกหล่นจากรอบแก้
  AC-NOSQL-016-4 ก่อนหน้า:
  1. `docs/02-design/01-prototypes/v2/scr-016.html` breadcrumb ยังโชว์
     "Test Case: TC-097 – TC-099" ทั้งที่เพิ่ง implement TC-100 (จำลองหน้าจอ
     ถูกลบ) เข้าไปแล้วในรอบก่อนหน้า — ช่วง Test Case ไม่ครอบคลุม TC-100
  2. `docs/02-design/01-prototypes/index.md` บรรทัดอ้างอิง `DESIGN.md` ยังใช้
     คำอธิบายเก่าตกค้าง "โทน Earth tone + Minimalist + Muji-inspired"
     (คำอธิบายจากตอนสร้างไฟล์ครั้งแรกสุด) ไม่ตรงกับโทนปัจจุบัน "Warm
     Terracotta Workspace" ที่อัปเดตไปแล้ว และบรรทัดเดียวกันยังระบุ v2 ว่า
     "(Draft)" ทั้งที่เพิ่งยืนยันเป็น Confirmed ไปแล้วในขั้นตอนก่อนหน้า
- เอกสารที่แก้ไข:
  - `docs/02-design/01-prototypes/v2/scr-016.html` (แก้ไข — breadcrumb เป็น
    "Test Case: TC-097 – TC-100")
  - `docs/02-design/01-prototypes/index.md` (แก้ไข — อัปเดตคำอธิบายโทน
    DESIGN.md ให้ตรงปัจจุบัน และแก้สถานะ v2 เป็น "(Confirmed)")
  - ทดสอบซ้ำในเบราว์เซอร์แล้วยืนยันว่า breadcrumb ของ `scr-016.html` แสดง
    "TC-097 – TC-100" ถูกต้องจริง
- สรุป: ตรวจครบทั้งเอกสาร technical index, test-plan index, test-cases
  index, prototypes index, และไฟล์ HTML ทั้ง 4 หน้าของ v2 แล้ว ไม่พบจุด
  หลุดสโคป/ไม่สอดคล้องกันอีกนอกจาก 2 จุดข้างต้นที่แก้ไปแล้ว — จุดที่เหลือเป็น
  แค่ของเสริมไม่บังคับ (เช่น `test-plan.md` ไฟล์ระบบเต็มยังไม่มีลิงก์ไขว้ไปยัง
  เอกสาร NoSQL scope ซึ่งเป็นเรื่อง discoverability ไม่กระทบการเขียนโค้ด)
