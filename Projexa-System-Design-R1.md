# Projexa — สรุปการออกแบบระบบ
**ระบบบริหารโครงการและเอกสารอัจฉริยะ (TOR-Driven Project & Document Management)**

เวอร์ชัน 0.1 — เอกสารสรุปแนวคิดและขอบเขตการพัฒนา

---

## 1. แนวคิดหลักของระบบ

Projexa เปลี่ยนแกนการทำงานจาก **"เอกสารเป็นศูนย์กลาง"** เป็น **"ข้อมูลเป็นศูนย์กลาง"**

ปัจจุบันทีมทำเอกสาร Word แยกไฟล์ แล้วคัดลอกข้อมูลไปมาระหว่าง Excel / LINE / Drive ทำให้ข้อมูลไม่ตรงกันและเสียเวลาซ้ำซ้อน

Projexa ใช้แนวคิดว่า **ข้อมูลถูกบันทึกครั้งเดียวในระบบ ส่วนเอกสารทุกฉบับคือ "มุมมอง" ของข้อมูลชุดเดียวกันที่ถูก generate ออกมา**

โดยมี **TOR เป็นจุดตั้งต้นของทุกอย่าง**

```
TOR (ข้อกำหนดโครงการ)
   ↓  AI สกัด + คนยืนยัน
ความต้องการ (REQ) + งวดงาน
   ↓  AI วิเคราะห์ + คนยืนยัน
หน้าจอ / Flow / เงื่อนไขการทำงาน
   ↓
แผนงาน + ผู้รับผิดชอบ + สถานะ + เอกสารแนบ
   ↓  AI ร่าง + Template render
เอกสารส่งมอบ (REQ / SDD / TSC / User Manual)
```

**หลักการที่ยึดตลอดทั้งระบบ**

| หลักการ | ความหมาย |
|---|---|
| Single Source of Truth | ข้อมูลหนึ่งอย่างบันทึกที่เดียว เอกสารดึงไปใช้ ไม่พิมพ์ซ้ำ |
| Full Traceability | ทุกหน้าจอ / test case / หัวข้อเอกสาร อ้างกลับไปยังข้อใน TOR ได้ |
| Human-in-the-loop | AI เสนอ คนตัดสิน ไม่มีข้อมูลใดเข้าระบบโดยไม่ผ่านการยืนยัน |
| Everything is Logged | ทุกการเปลี่ยนสถานะ / ผู้รับผิดชอบ บันทึก ใคร-เมื่อไหร่-ทำไม |
| Template Compliance | รูปแบบเอกสารคุมด้วย Template ขององค์กร ไม่ให้ AI จัดรูปแบบเอง |

---

## 2. ผู้ใช้งานและสิทธิ์

| Role | หน้าที่หลักในระบบ |
|---|---|
| Project Manager (PM) | สร้างโครงการ นำเข้า TOR อนุมัติแผนงาน มอบหมายงาน ดูภาพรวม สั่งสร้างเอกสารส่งมอบ |
| System Analyst (SA) | ยืนยันผลวิเคราะห์จาก TOR ออกแบบหน้าจอ Flow เงื่อนไขการทำงาน |
| Business Analyst (BA) | ยืนยัน/แก้ไขความต้องการ (REQ) ประสานงานกับผู้ใช้ |
| Developer (Dev) | อัปเดตสถานะการพัฒนาหน้าจอที่รับผิดชอบ แนบหลักฐาน |
| Tester | จัดการ Test Case บันทึกผลทดสอบ แจ้งปัญหา |
| Viewer | ดูอย่างเดียว (เช่น ผู้บริหาร เจ้าของโครงการ) |
| Admin | จัดการผู้ใช้ สิทธิ์ Template เอกสาร ข้อมูลตั้งต้น |

> หนึ่งคนมีได้หลาย Role และสิทธิ์กำหนดได้ระดับโครงการ

---

## 3. สถาปัตยกรรมระบบ

```
┌─────────────────────────────────────────────────────────┐
│  Presentation Layer  —  Web Application (Responsive)     │
├─────────────────────────────────────────────────────────┤
│  Application Layer                                        │
│  ┌──────────┬───────────┬───────────┬─────────────────┐  │
│  │ Project  │ Analysis  │ Tracking  │ Document        │  │
│  │ Service  │ Service   │ Service   │ Generator       │  │
│  └──────────┴───────────┴───────────┴─────────────────┘  │
├─────────────────────────────────────────────────────────┤
│  AI Orchestration Layer                                   │
│  TOR Parser │ Design Analyzer │ Doc Writer │ Test Gen     │
│  (ทุกตัวคืนค่าเป็น Structured JSON เท่านั้น)              │
├─────────────────────────────────────────────────────────┤
│  Data Layer     Database  │  File Storage  │  Audit Log   │
└─────────────────────────────────────────────────────────┘
```

**จุดออกแบบสำคัญ:** AI Layer แยกออกจาก Document Generator อย่างชัดเจน
AI ไม่เคยสร้างไฟล์ .docx เอง — AI คืนค่าเป็น JSON แล้ว Document Generator นำไปเติมลง Template

---

## 4. โครงสร้างข้อมูล (Data Model)

### 4.1 กลุ่มข้อมูลหลัก

| Entity | คำอธิบาย | ความสัมพันธ์สำคัญ |
|---|---|---|
| `Project` | ข้อมูลโครงการ | มี TOR, Milestone, Screen หลายรายการ |
| `TorDocument` | ไฟล์ TOR ต้นฉบับ + สถานะการสกัด | belongs to Project |
| `TorClause` | ข้อย่อยใน TOR (เลขข้อ, เนื้อหา, เลขหน้า) | belongs to TorDocument |
| `Requirement` | ความต้องการ REQ-xxx (FR/NFR) | อ้าง TorClause |
| `Milestone` | งวดงาน (ชื่อ, วันครบกำหนด, % ชำระเงิน) | มี DeliverableDoc |
| `DeliverableDoc` | เอกสารที่ต้องส่งในงวดนั้น (REQ/SDD/TSC/UM) | belongs to Milestone |
| `Module` | กลุ่มงาน/โมดูลของระบบที่จะพัฒนา | belongs to Project |
| `Screen` | หน้าจอ SCR-xxx (ชื่อ, ประเภท, คำอธิบาย) | อ้าง Requirement, belongs to Module |
| `ScreenCapability` | ความสามารถของหน้าจอ (เพิ่ม/แก้/ลบ/ค้นหา/พิมพ์/อนุมัติ) | belongs to Screen |
| `BusinessRule` | เงื่อนไขการทำงาน/validation | belongs to Screen |
| `Assignment` | ผู้รับผิดชอบ (Screen × Role × User) | belongs to Screen |
| `ScreenStatus` | สถานะปัจจุบัน + วันที่วางแผน/จริง | belongs to Screen |
| `StatusHistory` | ประวัติทุกการเปลี่ยนสถานะและผู้รับผิดชอบ | **audit trail** |
| `Attachment` | ไฟล์แนบ ผูกกับหน้าจอ + ขั้นตอน | belongs to Screen/Stage |
| `TestCase` | TC-xxx (เงื่อนไข, ขั้นตอน, ผลคาดหวัง) | อ้าง Screen |
| `TestResult` | ผลการทดสอบแต่ละรอบ | belongs to TestCase |
| `Issue` | ปัญหาและแนวทางแก้ไข | อ้าง Screen หรือ Project |
| `DocTemplate` | แม่แบบเอกสาร .dotx + placeholder mapping | ระดับองค์กร |
| `GeneratedDocument` | เอกสารที่ระบบสร้าง (เวอร์ชัน, งวด, ผู้สร้าง) | อ้าง Milestone + DocTemplate |

### 4.2 เส้น Traceability (หัวใจของระบบ)

```
TorClause ──▶ Requirement ──▶ Screen ──▶ TestCase ──▶ TestResult
   3.2.1        REQ-014       SCR-009      TC-041      Pass
                    │             │
                    └─────────────┴──▶ หัวข้อในเอกสาร REQ / SDD / TSC
```

ถ้าเส้นนี้ครบ ระบบจะออก **Requirement Traceability Matrix (RTM)** ได้อัตโนมัติ
ซึ่งเป็นสิ่งที่ Jira / Trello / MS Project ให้ไม่ได้ และเป็นจุดต่างที่ชัดที่สุดของ Projexa

---

## 5. โมดูลและรายการหน้าจอ

รวม **26 หน้าจอ** ใน 7 โมดูล

### M1 — จัดการโครงการและ TOR

| รหัส | หน้าจอ | ความสามารถหลัก |
|---|---|---|
| SCR-001 | Dashboard ภาพรวม | สรุปโครงการ ความก้าวหน้า งานใกล้ครบกำหนด งานเลยกำหนด |
| SCR-002 | รายการโครงการ | ค้นหา กรอง แบ่งหน้า |
| SCR-003 | ข้อมูลโครงการ | เพิ่ม/แก้ไข ชื่อ ลูกค้า ระยะเวลา งบประมาณ ทีมงาน |
| SCR-004 | นำเข้า/บันทึก TOR | อัปโหลด .docx/.pdf หรือกรอกเอง แสดงสถานะการสกัด |
| SCR-005 | ตรวจสอบผลสกัด TOR | แสดงผล AI เทียบข้อความต้นฉบับ ยืนยัน/แก้ไข/เพิ่ม/ลบ รายข้อ |
| SCR-006 | ทะเบียนความต้องการ (REQ) | list + detail, แยก FR/NFR, ลิงก์ไปข้อใน TOR |
| SCR-007 | งวดงานและเอกสารส่งมอบ | กำหนดงวด วันครบกำหนด เอกสารที่ต้องส่งแต่ละงวด |

### M2 — วิเคราะห์และออกแบบ

| รหัส | หน้าจอ | ความสามารถหลัก |
|---|---|---|
| SCR-008 | ผลวิเคราะห์ออกแบบจาก AI | แสดงข้อเสนอ Flow/หน้าจอ พร้อมข้ออ้างอิง TOR ให้ยืนยันรายรายการ |
| SCR-009 | ทะเบียนหน้าจอ | list หน้าจอทั้งหมด แยกตามโมดูล สถานะ ผู้รับผิดชอบ |
| SCR-010 | รายละเอียดหน้าจอ | ชื่อ ประเภท คำอธิบาย ความสามารถ เงื่อนไขการทำงาน field list ลิงก์ REQ |
| SCR-011 | ผังการทำงาน (Flow) | แสดง flow และการเชื่อมโยงระหว่างหน้าจอ |

### M3 — วางแผนและมอบหมายงาน

| รหัส | หน้าจอ | ความสามารถหลัก |
|---|---|---|
| SCR-012 | แผนงาน / Timeline | มุมมอง Gantt ตามงวดงานและหน้าจอ ปรับวันที่ได้ |
| SCR-013 | มอบหมายผู้รับผิดชอบ | กำหนด SA / BA / Dev / Tester รายหน้าจอ มอบหมายเป็นชุดได้ |
| SCR-014 | ปฏิทินและการแจ้งเตือน | แจ้งเตือนงานใกล้ครบกำหนด/เลยกำหนด ตั้งค่าช่องทางแจ้งเตือน |

### M4 — ติดตามการดำเนินงาน

| รหัส | หน้าจอ | ความสามารถหลัก |
|---|---|---|
| SCR-015 | กระดานติดตามสถานะ | Kanban ตาม stage ลากเปลี่ยนสถานะได้ |
| SCR-016 | บันทึกความก้าวหน้า | บันทึกสถานะ วันที่จริง % ความคืบหน้า หมายเหตุ แนบไฟล์รายขั้นตอน |
| SCR-017 | ประวัติการเปลี่ยนแปลง | Timeline การเปลี่ยนสถานะ/ผู้รับผิดชอบ พร้อมเหตุผล ย้อนหลังได้ทั้งหมด |
| SCR-018 | ติดตามปัญหา (Issue) | บันทึกปัญหา ผลกระทบ แนวทางแก้ไข ผู้รับผิดชอบ สถานะ |

### M5 — จัดการการทดสอบ

| รหัส | หน้าจอ | ความสามารถหลัก |
|---|---|---|
| SCR-019 | ทะเบียน Test Case | list/detail, สร้างเองหรือให้ AI ร่างจากหน้าจอ + business rule |
| SCR-020 | บันทึกผลการทดสอบ | บันทึกผลรายรอบ Pass/Fail แนบหลักฐาน ผูกกับ Issue |

### M6 — จัดการเอกสาร

| รหัส | หน้าจอ | ความสามารถหลัก |
|---|---|---|
| SCR-021 | คลังเอกสารโครงการ | รวมไฟล์ทั้งหมด ค้นหา กรองตามงวด/หน้าจอ/ประเภท |
| SCR-022 | จัดการ Template เอกสาร | อัปโหลด .dotx กำหนด placeholder mapping ทดสอบ render |
| SCR-023 | สร้างเอกสารส่งมอบ | เลือกงวด + ประเภทเอกสาร → preview → แก้ไข → export .docx |
| SCR-024 | ประวัติเวอร์ชันเอกสาร | เก็บทุกเวอร์ชัน เทียบความต่าง ดาวน์โหลดย้อนหลัง |

### M7 — ผู้ดูแลระบบ

| รหัส | หน้าจอ | ความสามารถหลัก |
|---|---|---|
| SCR-025 | ผู้ใช้งานและสิทธิ์ | จัดการผู้ใช้ กำหนด Role ระดับโครงการ |
| SCR-026 | ข้อมูลตั้งต้น | ประเภทเอกสาร ประเภทหน้าจอ สถานะ ตำแหน่งงาน |

---

## 6. วงจรสถานะการพัฒนา (Screen Lifecycle)

```
Not Started → Analysis → Design → Development → Unit Test → SIT → UAT → Done
                                        ↕
                                     On Hold / Rework
```

**ทุกครั้งที่เปลี่ยนสถานะ ระบบบังคับบันทึก**

- ผู้ทำรายการ และวันเวลา
- สถานะเดิม → สถานะใหม่
- ผู้รับผิดชอบเดิม → ผู้รับผิดชอบใหม่ (ถ้ามีการเปลี่ยน)
- **เหตุผล** (บังคับกรอกเมื่อเลื่อนวันหรือถอยสถานะ)
- ไฟล์แนบประกอบขั้นตอนนั้น

ข้อมูลชุดนี้คือคำตอบโดยตรงของปัญหาข้อ 2 ในเอกสาร REQ-2 (ตรวจสอบประวัติการเปลี่ยนผู้รับผิดชอบและสาเหตุความล่าช้า) และยังนำไป generate เป็นเนื้อหา "รายงานความก้าวหน้า" ได้ทันที

---

## 7. การออกแบบส่วน AI

### 7.1 AI Component

| Component | Input | Output (JSON) | ผู้ยืนยัน |
|---|---|---|---|
| **TOR Parser** | ข้อความจาก TOR | ขอบเขตงาน, งวดงาน, เอกสารส่งมอบ, FR, NFR, ข้อจำกัด — แต่ละรายการมี `source_clause` | PM / BA |
| **Design Analyzer** | REQ ที่ยืนยันแล้ว | Actor, Flow, รายการหน้าจอ, ความสามารถ, business rule — แต่ละรายการมี `source_req` | SA |
| **Test Generator** | Screen + BusinessRule | Test Case (เงื่อนไข, ขั้นตอน, ผลคาดหวัง) | Tester |
| **Document Writer** | ข้อมูลจริงในระบบ | เนื้อความบรรยายรายหัวข้อ | PM / SA |

### 7.2 กติกาที่บังคับกับ AI ทุกตัว

1. **คืนค่าเป็น Structured JSON เท่านั้น** ห้ามคืนเป็นข้อความอิสระหรือไฟล์เอกสาร
2. **ทุกรายการต้องมีข้ออ้างอิงต้นทาง** ถ้าอ้างไม่ได้ ให้ติดป้าย `suggested` และแยกให้คนพิจารณาต่างหาก
3. **ห้ามเขียนตัวเลขเอง** — ตัวเลขความก้าวหน้า วันที่ จำนวนหน้าจอ สถานะ ดึงจากฐานข้อมูลจริงเสมอ
4. **แสดง confidence** ให้ผู้ใช้เห็นว่ารายการใดควรตรวจละเอียด
5. **ทุก output ต้องผ่านหน้าจอ Review ก่อนบันทึกจริง** ระบบแยกป้ายกำกับ `AI Generated` / `Human Confirmed`

---

## 8. การออกแบบส่วนสร้างเอกสาร

### 8.1 กระบวนการ

```
1. ผู้ใช้เลือก งวดงาน + ประเภทเอกสาร (REQ / SDD / TSC / User Manual)
2. ระบบดึงข้อมูลจริงจากฐานข้อมูล  ← ส่วนที่เป็นตาราง ตัวเลข รายการ
3. ส่งข้อมูลให้ AI ร่างเฉพาะ "เนื้อความบรรยาย"
4. รวมเป็น JSON เดียว → เติมลง Template .dotx
5. Preview บนหน้าจอ → ผู้ใช้แก้ไขได้
6. Export เป็น .docx + บันทึกเวอร์ชัน
```

### 8.2 การแบ่งงานระหว่างระบบกับ AI

| ระบบสร้างเอง (ถูกต้อง 100%) | AI ร่างให้ (ต้องตรวจ) |
|---|---|
| เลขอ้างอิง REQ / SCR / TC | ความเป็นมาและวัตถุประสงค์ |
| สารบัญ เลขหน้า หัว-ท้ายกระดาษ | คำบรรยายความต้องการแต่ละข้อ |
| ตารางความต้องการ ตารางหน้าจอ | คำอธิบายการทำงานของหน้าจอ |
| Requirement Traceability Matrix | ขั้นตอนในคู่มือผู้ใช้ |
| ตารางสถานะ วันที่ ความก้าวหน้า | บทสรุปความก้าวหน้าและความเสี่ยง |
| รูปแบบ ฟอนต์ heading ตามองค์กร | — |

### 8.3 โครงสร้าง Template

Template หนึ่งฉบับประกอบด้วย

- ไฟล์ `.dotx` ที่มี placeholder เช่น `{{project.name}}`, `{{#requirements}}...{{/requirements}}`
- ตาราง mapping ระบุว่า placeholder ใด ดึงจาก data path ใด
- จุดกำหนดตำแหน่งแทรกรูปภาพ (diagram, wireframe, screenshot) ที่ผู้ใช้แนบเอง

> **ข้อจำกัดที่ต้องยอมรับ:** AI สร้างรูป diagram / wireframe / screenshot ไม่ได้ ระบบต้องมีช่องให้แนบรูปและกำหนดตำแหน่งใน template

---

## 9. Workflow หลักของระบบ (End-to-End)

```
[PM]     สร้างโครงการ → อัปโหลด TOR
   ↓
[AI]     สกัดขอบเขต งวดงาน ความต้องการ ข้อจำกัด
   ↓
[BA/PM]  ตรวจสอบและยืนยันผลสกัด  ◀── Gate 1
   ↓
[AI]     วิเคราะห์เป็น Flow + รายการหน้าจอ + เงื่อนไขการทำงาน
   ↓
[SA]     ตรวจสอบและยืนยัน ปรับแก้ เพิ่มหน้าจอ  ◀── Gate 2
   ↓
[PM]     จัดทำแผนงาน กำหนดวันที่ มอบหมาย SA/BA/Dev/Tester
   ↓
[ทีม]    ดำเนินงาน → อัปเดตสถานะ แนบเอกสารแต่ละขั้น → ระบบ log ทุกครั้ง
   ↓
[Tester] สร้าง/ยืนยัน Test Case → บันทึกผลทดสอบ → แจ้ง Issue
   ↓
[PM]     ถึงงวดงาน → สั่งสร้างเอกสาร → preview → แก้ไข → export .docx
   ↓
         ส่งมอบ + เก็บเวอร์ชันไว้ในระบบ
```

---

## 10. ขอบเขตการพัฒนาสำหรับโครงการ RAISE

พัฒนาแบบ **ลึกหนึ่งเส้น** ให้เห็น traceability ครบวงจร ดีกว่ากว้างแต่ตื้น

### อยู่ในขอบเขต (Must Have — 13 หน้าจอ)

| ลำดับ | หน้าจอ | เหตุผล |
|---|---|---|
| 1 | SCR-001 Dashboard | ภาพรวมสำหรับสาธิต |
| 2 | SCR-003 ข้อมูลโครงการ | จุดเริ่มต้น |
| 3 | SCR-004 นำเข้า TOR | จุดขายหลัก |
| 4 | SCR-005 ตรวจสอบผลสกัด TOR | แสดง Human-in-the-loop |
| 5 | SCR-006 ทะเบียนความต้องการ | ต้นทางของ traceability |
| 6 | SCR-007 งวดงาน | ผูกกับเอกสารส่งมอบ |
| 7 | SCR-008 ผลวิเคราะห์ออกแบบจาก AI | จุดขายหลัก |
| 8 | SCR-009 ทะเบียนหน้าจอ | แกนกลางของการติดตาม |
| 9 | SCR-010 รายละเอียดหน้าจอ | เก็บ capability + business rule |
| 10 | SCR-013 มอบหมายผู้รับผิดชอบ | ตรงตามความต้องการ |
| 11 | SCR-016 บันทึกความก้าวหน้า + แนบไฟล์ | ตรงตามความต้องการ |
| 12 | SCR-017 ประวัติการเปลี่ยนแปลง | แก้ปัญหาข้อ 2 โดยตรง |
| 13 | SCR-023 สร้างเอกสารส่งมอบ | ผลลัพธ์ปลายทาง |

**เอกสารที่ demo การสร้าง:** REQ + รายงานความก้าวหน้า (2 ประเภท ทำให้สมบูรณ์)

### นอกขอบเขตเฟสนี้ (Phase 2)

- SDD / TSC / คู่มือผู้ใช้ (ออกแบบโครงสร้างรองรับไว้ แต่ยังไม่ทำ template)
- Gantt แบบแก้ไขได้ (SCR-012), Kanban (SCR-015)
- โมดูลทดสอบเต็มรูปแบบ (M5)
- เชื่อมต่อ LINE Notify / Email
- เทียบความต่างระหว่างเวอร์ชันเอกสาร (SCR-024)

### ข้อจำกัดที่ประกาศไว้ล่วงหน้า

1. รองรับ TOR ที่เป็นไฟล์ .docx / .pdf ที่มี text layer เท่านั้น (ไฟล์สแกนต้อง OCR — ไว้เฟสถัดไป)
2. AI ช่วยร่าง ไม่ใช่ตัดสินใจแทน ทุกผลลัพธ์ต้องผ่านการยืนยัน
3. รูป diagram / wireframe ต้องแนบเอง
4. รองรับภาษาไทยเป็นหลัก

---

## 11. เทคโนโลยีที่เสนอ

| ส่วน | เทคโนโลยีที่เสนอ | หมายเหตุ |
|---|---|---|
| Frontend | React / Next.js + TypeScript | Responsive |
| Backend | Node.js (NestJS) หรือ Python (FastAPI) | เลือกตามความถนัดทีม |
| Database | PostgreSQL | ต้องการ JSONB สำหรับเก็บผลสกัด AI |
| File Storage | MinIO / S3-compatible | เก็บ TOR, ไฟล์แนบ, เอกสารที่สร้าง |
| อ่าน TOR | python-docx, pdfplumber | ถ้า backend เป็น Node ให้แยกเป็น service เล็ก |
| สร้าง .docx | **python-docx + docxtpl** | รองรับ .dotx และ placeholder loop |
| AI | Claude API | เรียกผ่าน AI Orchestration Layer |
| Auth | JWT + RBAC | สิทธิ์ระดับโครงการ |

> **หมายเหตุ:** งานสร้าง .docx แนะนำให้เป็น Python service เสมอ เพราะ ecosystem ด้าน Word บน Python สมบูรณ์กว่ามาก

---

## 12. แผนการพัฒนา (แบ่งเฟส)

| เฟส | ระยะเวลา | สิ่งที่ได้ |
|---|---|---|
| **P1 — วางฐาน** | 3 สัปดาห์ | Data model, Auth/RBAC, จัดการโครงการ, นำเข้า TOR (เก็บไฟล์+แตก clause) |
| **P2 — AI สกัด TOR** | 3 สัปดาห์ | TOR Parser, หน้าจอ Review, ทะเบียน REQ, งวดงาน |
| **P3 — AI วิเคราะห์ออกแบบ** | 3 สัปดาห์ | Design Analyzer, ทะเบียนหน้าจอ, รายละเอียดหน้าจอ, traceability |
| **P4 — ติดตามงาน** | 3 สัปดาห์ | มอบหมายผู้รับผิดชอบ, สถานะ, ไฟล์แนบ, audit trail, Dashboard |
| **P5 — สร้างเอกสาร** | 4 สัปดาห์ | Template engine, Document Writer, สร้าง REQ + รายงานความก้าวหน้า |
| **P6 — ทดสอบและสาธิต** | 2 สัปดาห์ | ทดสอบ ปรับแต่ง เตรียมข้อมูลสาธิต วัดผล |

รวมประมาณ **18 สัปดาห์**

---

## 13. ตัวชี้วัดความสำเร็จ

| ตัวชี้วัด | เกณฑ์ | วิธีวัด |
|---|---|---|
| ลดเวลาจัดทำรายงานความก้าวหน้า | ลด ≥ 50% | จับเวลาแบบเดิม (baseline) เทียบกับใช้ระบบ วัดจากผู้ใช้จริง ≥ 3 คน |
| ความถูกต้องของการสกัด TOR | ความต้องการที่ AI สกัดถูกต้อง ≥ 70% | นับจำนวนรายการที่ผู้ใช้ยืนยันโดยไม่แก้ ÷ รายการทั้งหมด |
| คุณภาพเอกสารที่สร้าง | ผู้ใช้แก้ไขเนื้อหาไม่เกิน 30% | วัดจากสัดส่วนเนื้อหาที่ถูกแก้ใน preview |
| ความครบถ้วนของ traceability | ทุกหน้าจอสาวกลับไป TOR ได้ 100% | ตรวจจาก RTM ที่ระบบสร้าง |
| ติดตามสถานะจากระบบเดียว | ใช้งานได้จริงตลอดโครงการนำร่อง | ผู้ใช้ยืนยัน + สถิติการใช้งาน |

> **สิ่งที่ต้องทำก่อนเริ่มพัฒนา:** เก็บ baseline เวลาการจัดทำเอกสารแบบเดิม (ชั่วโมง/งวดงาน) มิฉะนั้นจะพิสูจน์ "ลด 50%" ไม่ได้

---

## 14. ความเสี่ยงและแนวทางรับมือ

| ความเสี่ยง | ผลกระทบ | แนวทางรับมือ |
|---|---|---|
| TOR แต่ละฉบับรูปแบบต่างกันมาก | สกัดไม่แม่น | ออกแบบ Review Gate ให้แก้ไขง่าย + เก็บตัวอย่าง TOR หลายรูปแบบมาทดสอบ |
| AI เสนอหน้าจอเกินจริง | ขอบเขตงานบานปลาย | บังคับอ้าง source clause ถ้าอ้างไม่ได้ให้ติดป้าย `suggested` |
| เอกสารที่สร้างไม่ตรงรูปแบบองค์กร | ใช้งานจริงไม่ได้ | ใช้ .dotx จริงขององค์กรมาทำ template ตั้งแต่ต้น |
| ทีมไม่อัปเดตสถานะในระบบ | ข้อมูลไม่ทันสมัย เอกสารผิด | ทำหน้าจออัปเดตให้เร็วมาก (≤ 3 คลิก) + แจ้งเตือนอัตโนมัติ |
| ขอบเขต demo ใหญ่เกินไป | ไม่เสร็จทัน | ยึด 13 หน้าจอใน Must Have อย่างเคร่งครัด |

---

## 15. สรุปจุดต่างจากระบบที่มีอยู่

| ความสามารถ | Jira / Trello | MS Project | **Projexa** |
|---|---|---|---|
| ติดตามงานและสถานะ | ✅ | ✅ | ✅ |
| Timeline / Gantt | ⚠️ (ปลั๊กอิน) | ✅ | ✅ |
| นำเข้า TOR แล้ววิเคราะห์อัตโนมัติ | ❌ | ❌ | ✅ |
| แตก TOR เป็นรายการหน้าจอที่ต้องพัฒนา | ❌ | ❌ | ✅ |
| Traceability TOR → REQ → หน้าจอ → Test | ⚠️ (ต้องตั้งค่าเอง) | ❌ | ✅ |
| สร้างเอกสาร Word ตามรูปแบบองค์กร | ❌ | ❌ | ✅ |
| ผูกเอกสารส่งมอบกับงวดงาน | ❌ | ⚠️ | ✅ |
| AI ร่างเนื้อหาจากข้อมูลจริงในระบบ | ❌ | ❌ | ✅ |

**ประโยคเดียวที่อธิบาย Projexa:**
> ระบบที่รับ TOR เข้าไปตอนต้นโครงการ แล้วส่งเอกสารส่งมอบออกมาตอนจบงวดงาน โดยไม่ต้องพิมพ์ข้อมูลซ้ำแม้แต่ครั้งเดียว
